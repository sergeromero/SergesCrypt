const gameRepository = require('./gameRepository');

describe("gemeRepository", () => {
    it("returns a game", () => {
        /*
        const game = gameRepository.getGame(0);

        return game.then(g => expect(g.title).toBe("The Dark House"));
        */
    });
});